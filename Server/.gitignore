node_modules
# Keep environment variables out of version control
.env
.env.test

/src/generated/prisma

# Uploaded case documents / avatars — user content, not source
/uploads/

# Manual curl-testing cookie jars (this codebase's docs/plans use this filename
# convention) — may contain live JWT tokens, must never be committed
cookies.txt
